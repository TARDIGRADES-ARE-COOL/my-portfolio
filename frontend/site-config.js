(() => {
  // Leave empty to use direct GitHub API for project data.
  // Set this to your deployed backend URL when you want chat/API via backend.
  // Example: "https://your-backend.onrender.com"
  window.PORTFOLIO_API_BASE = "";
})();
